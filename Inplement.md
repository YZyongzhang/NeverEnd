# Inplement
## example
### 函数名: Helloworld
1. 定义: int helloworld(int a , int b);
2. 解释: a: int 类型， b int 类型
3. 函数说明：该函数返回输入的两个int值a与b的和
4. 返回值：成功则返回和，失败则返回零
5. 示例：无（一般可不写，比较复杂的函数需要写出示例的传参比如：示例：a=1,b=3）